import java.util.Arrays;
import java.util.List;
import java.util.Scanner;
import java.util.stream.Collectors;

public class Train2 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        List<String> wagons = Arrays.stream(sc.nextLine().split(" ")).collect(Collectors.toList());
        int maxCapacity = Integer.parseInt(sc.nextLine());
        String command = sc.nextLine();

        int totalPassengers = 0;

        while (!command.equals("end")) {

            List<String> commandLine = Arrays.stream(sc.nextLine().split(" ")).collect(Collectors.toList());
            String add = commandLine.get(0);

            int passengers = 0;
            if (add.equals("Add")){
                int addWagons =Integer.parseInt(commandLine.get(1));
                wagons.add(addWagons);
                continue;
            }else{
                 passengers = Integer.parseInt(add);
            }
                    //32 54 21 12 4 0 23 + 10 + 0
                    //30 10 75
            for (int i = 0; i < wagons.size() -1; i++) {
                int currentPassengers = Integer.parseInt(wagons.get(i));
                            totalPassengers = currentPassengers + passengers;

                                if (currentPassengers < maxCapacity && totalPassengers < maxCapacity){
                                   // totalPassengers = currentPassengers + passengers;
                                    wagons.set(i, String.valueOf(totalPassengers));
                                    i =-1;

                                }else{
                                    totalPassengers = Math.abs(totalPassengers - maxCapacity);
                                    wagons.set(i+1, String.valueOf(totalPassengers) + String.valueOf(wagons.get(i+1)));
                                }
                command = sc.nextLine();
                    }


            }

           for(String el : wagons){
               System.out.print(el + " ");
           }
        }
    }

