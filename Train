import java.util.Arrays;
import java.util.List;
import java.util.Scanner;
import java.util.stream.Collectors;

public class Train2 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        List<String> wagons = Arrays.stream(sc.nextLine().split(" ")).collect(Collectors.toList());
        int maxCapacity = Integer.parseInt(sc.nextLine());
        String command = sc.nextLine();

           while (!command.equals("end")){
               List<String> commandLine = Arrays.stream(command.split(" ")).collect(Collectors.toList());
               int addWagons = 0;

                    if (commandLine.get(0).equals("Add")) {
                        addWagons = Integer.parseInt(commandLine.get(1));
                             wagons.add(String.valueOf(addWagons));
                             command = sc.nextLine();
                             continue;
                    }
              int passengers = Integer.parseInt(commandLine.get(0));

               for (int i = 0; i < wagons.size() -1; i++) {
                   int currentWagon = Integer.parseInt(wagons.get(i));
                   if (passengers >= 75 && currentWagon == 0){
                       wagons.set(i, String.valueOf(passengers));
                   }
               }
               command = sc.nextLine();
           }
        }
    }

