import java.util.Arrays;
import java.util.Collections;
import java.util.List;
import java.util.Scanner;
import java.util.stream.Collectors;

public class ChangeList {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        List<Integer> numsList = Arrays.stream(sc.nextLine().split(" ")).map(Integer::parseInt).collect(Collectors.toList());
        String command = sc.nextLine();

        while (!command.equals("end")){

            String[] manipulate = sc.nextLine().split(" ");

            if (manipulate[0].equals("Delete")){
                int element = Integer.parseInt(manipulate[1]);
                numsList.removeAll(Collections.singleton(element));

            }else if (manipulate[0].equals("Insert")){
                int element = Integer.parseInt(manipulate[1]);
                int position = Integer.parseInt(manipulate[2]);

                numsList.set(position, element);

            }
            command = sc.nextLine();
        }

       for (int el : numsList){
           System.out.print(el + " ");
       }
    }

}
